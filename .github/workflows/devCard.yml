# This is a basic workflow to help you get started with Actions

name: DevCard


permissions:
  contents: write

on:
  workflow_dispatch:
  push:
    branches:
      - main
  schedule:
    - cron: "0 0 * * *"
jobs:
  devcard:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: devcard
        uses: dailydotdev/action-devcard@3.1.0
        with:
          user_id: ${{ secrets.DEVCARD_ID }}
          commit_message: "chore: update ${filename}" 
          commiter_email: "aksharpatel1233@gmail.com"
          commiter_name: "AksharP5"
